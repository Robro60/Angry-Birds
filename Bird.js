class Bird extends Base {

    
    constructor(x,y){

        super(x,y,65,65);
        this.image = loadImage("bird.png")
    }

    display(){

       

        super.display();
    }
}